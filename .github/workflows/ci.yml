name: CI

on:
  push:
    branches: [main, master]
    tags:
      - "v*"          # also run CI on tag pushes so a release never
                      # ships from a red build
  pull_request:
    branches: [main, master]
  workflow_dispatch:

# Opt into Node.js 24 for the JS-based actions (checkout, setup-python).
# Node 20 will be removed from runners on 2026-09-16; setting this avoids
# the deprecation banner now and forces our chosen actions onto Node 24.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install package + dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]" ruff

      - name: Lint with ruff
        run: ruff check .

      - name: Run tests
        run: pytest -q --tb=short

      - name: Verify entry points are importable
        run: |
          python -c "import bot; bot.main"
          python -c "import mcp_server; mcp_server.main"
          python -c "from cascade.core import run_cascade; print('core ok')"
