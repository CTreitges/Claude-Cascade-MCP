# Local-only secrets and runtime config
.env
.env.local
secrets.env
*.secrets.env

# Per-machine cascade home / runtime artifacts
store/*.db
store/*.db-journal
store/*.log
store/*.jsonl
store/debug.log*
store/telegram.log*
store/errors.log*
store/cascade_session.json

# Per-task workspaces — always machine-local
workspaces/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
